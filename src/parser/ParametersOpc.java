package parser;

public class ParametersOpc extends Parameters{
    public boolean ParametersOpc(){
        if (Parameters()) {
            return true;
        } else {
            return true;
        }
    }
}
